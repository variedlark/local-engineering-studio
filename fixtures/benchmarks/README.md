# Benchmark Fixtures

This folder stores deterministic benchmark scenarios used for regression checks.

- `scenarios/medium-grid.json`: baseline medium-size graph/layout case.

Future scenarios should include:

- dense obstacle routing
- high-fanout graph connectivity
- long autosave + recovery replay
